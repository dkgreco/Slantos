<!--
<div class="row" style="background-color: rgba(239,231,215,0.9); border-radius: 15px; padding: 13px;">
    <div class="col-md-12" style="margin: 0 0 15px 0;">
        by <strong>
            <%= comment.author.username %>
        </strong>
            <% if (comment.postDate > 1) { %>
                <% var months = ['January', 'February', 'March', 'April']; %>
                <span> on
                    <%= comment.created.getDate() %>
                    <%= months[comment.created.getMonth()] %>,
                    <%= comment.created.getFullYear() %>
                </span>
                <div>
                    <strong>Rating:</strong> <%= comment.rating %> Stars
                </div>
            <% } else { %>
                <span>Today</span>
                <div>
                    <strong>Rating:</strong> <%= comment.rating %> Stars
                </div>
            <% }%>
        </div>
        <div class="col-md-12" style="margin: 0 0 15px 0;">
            <p><%= comment.text %></p>
        </div>

    </div>
    <% if ((index + 1) === inq.comments.length) {} else { %>
        <hr/>
    <% } %>

<% }); %>
<% } else { %>

<% } %>
<% } else { %>

<% } %>-->



